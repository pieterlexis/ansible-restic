---

restic_install_method: github
restic_install_path: '/usr/local/bin'
restic_password_path: '/var/lib/restic'
restic_url: 'https://github.com/restic/restic/releases/download/v{{ restic_version }}/restic_{{ restic_version }}_{{ ansible_system | lower() }}_{{ _restic_arch[ansible_userspace_architecture] | default(ansible_userspace_architecture) }}.bz2'

restic_version: '0.8.0'
